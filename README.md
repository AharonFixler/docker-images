# docker-images

Modular Docker images with staged builds and CI/CD.

This repository contains a growing set of Docker images built incrementally
(base + capabilities), published to Docker Hub under the `aharonfixler` namespace.
