# Images

Each directory under this path represents a Docker image.

Images are built incrementally using a base + capabilities model, for example:

- ubuntu
- ubuntu_ssh
- ubuntu_xrdp
- ubuntu_ssh_xrdp
